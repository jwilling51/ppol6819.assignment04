---
title: "Exercise 05, 06, and 07"
authors: 
- "Sarah Krause"
- "Miguel Villa"
- "Johnny Willing"
format: html
embed-resources: true
date: today
editor: visual
---

## Exercise 05: Loading in Custom Package

```{r}
library(ppol6819.assignment04)
set.seed(20260224)
```

## Exercise 06

```{r}
library(purrr)
library(tidyverse)

# Iterate ten toin cosses, 10,000 times and count number of heads.
heads <- numeric(10000)

for(i in 1:10000) {
  
  # N is number of flips, P is probability of heads. 
  toss <- cointoss(n = 10, p = 0.6)
  # Counts # heads in the iteration
  heads[i] <- sum(toss == 1) 
 
  # Creating a tibble for visualization
  result <- tibble(heads = heads,
                   iteration = 1:10000)
}

# Histogram
ggplot(result, aes(x = heads)) +
  geom_histogram() +
  theme_minimal()
```

## Exercise 07

```{r}
library(purrr)
library(tidyverse)

# Random walk with 10,000 steps with equal likelihood of moving either 1 step forward or 3 back.
walk <- random_walk(nsteps = 10000,
            step_one = 1, 
            step_two = -3,
            prob = c(0.5, 0.5))
# Visualization 
walk |>
  ggplot(aes(x = step_number, y = location)) +
  geom_line() +
  geom_hline(yintercept = 0) +
  theme_minimal()
  
```
